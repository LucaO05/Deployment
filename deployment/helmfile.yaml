releases:
  - name: mysql
    namespace: loestreich
    createNamespace: true
    chart: ./mysql
    values:
      - values.yaml

  - name: mywordpress
    namespace: loestreich
    chart: ./wordpress
    needs:
      - loestreich/mysql
    values:
      - values.yaml
