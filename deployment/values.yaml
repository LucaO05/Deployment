replicaCount: 1

wordpressUser: admin
wordpressPW: admin123
wordpressEmail: admin@example.com
wordpressFirstName: Admin
wordpressLastName: User
wordpressBlogName: "My WordPress"

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

persistence:
  size: 1Gi

postgresql:
  enabled: true
  auth:
    username: wpuser
    password: wppassword
    database: wordpress
  image:
    tag: 15.4
  service:
    ports:
      postgresql: "5432"

database:
  host: postgresql
  port: 5432
  name: wordpress
  user: wpuser
  password: wppassword

resources:
  limits:
    cpu: 0.5
    memory: 512Mi
  request:
    cpu: 0.1
    memory: 256Mi

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts:
    - host: mywordpress.i01-8.m3-dev.services
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: mywordpress-tls
      hosts:
        - mywordpress.i01-8.m3-dev.services
