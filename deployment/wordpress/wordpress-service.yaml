apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-wordpress
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-wordpress
  ports:
   - port: 80
     targetPort: 80